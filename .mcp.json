{
  "mcpServers": {
    "cold-box": {
      "type": "stdio",
      "command": "${CLAUDE_PROJECT_DIR}/bin/cold-box-mcp-stdio",
      "env": {
        "EVIDENCE_ROOT": "${EVIDENCE_ROOT:-/evidence}",
        "CASE_OUTPUT": "${CASE_OUTPUT:-/cases}",
        "VOL3": "${CLAUDE_PROJECT_DIR}/bin/vol",
        "PREFETCH_PARSER": "${CLAUDE_PROJECT_DIR}/tools/parse_prefetch.py"
      }
    }
  }
}
